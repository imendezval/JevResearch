[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "jevresearch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
jevresearch = "jevresearch.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
